server:
  port: 8081

spring:
  rabbitmq:
    routing-key: rk.consumer.rabbitmq

api:
  transactions:
    endpoint: http://localhost:8080/transactions
  transfer:
    endpoint: http://localhost:8080/transfer